# Server-side or local development secrets belong in an untracked .env file.
# The browser uses backend-config.js with only a public Supabase project URL
# and publishable key. Never add SUPABASE_SERVICE_ROLE_KEY to browser code.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

